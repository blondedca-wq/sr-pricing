# sr-pricing reads exactly two secrets, the same two every other runner on the droplet already has.
# install.sh copies them from /opt/sr-voice-bridge/.env (or another runner) so nobody has to type a key.
SUPABASE_URL=https://tjmxqyuzglcfnhgwamit.supabase.co
SUPABASE_SERVICE_KEY=PASTE_YOUR_KEY_HERE
# Optional: only run for these tenants (comma separated). Default: every tenant with photo_to_quote switched on.
# TENANT_IDS=
